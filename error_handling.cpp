//
// Created by admin on 2023-07-19.
//
// out_of_range

// try {} catch {}
// throw an error indicator when failure is normal
// throw an exception when the error is rare
// terminate when the error is unrecoverable
// assert(condition && "message")
// constexpr (evaluate something at compile time, not at run time)
// static_asset(condition, message)
// noexcept // only written for functions where no exceptions can ever be thrown